# Sample data

Do not commit real private photos or RAW files here.

Accepted future sample data should be:

- synthetic, or
- public-domain / CC0, or
- deliberately created test files with non-private coordinates, or
- tiny metadata-only fixtures if technically useful.

For normal testing, keep real photos in a local ignored folder such as:

```text
indata/
```

or temporarily outside the repository.
